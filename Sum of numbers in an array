#include<stdio.h>
int main()
{
    int i,n,a[10],sum=0;
    printf("enter n value");
    scanf("%d",&n);
    printf("enter %d elements\n",n);
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
     for(i=0;i<n;i++)
    sum=sum+a[i];
    printf("The sum is:%d",sum);
    return 0;
}
